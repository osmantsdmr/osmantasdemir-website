---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
---

<Layout title="Hizmetler | Osman Taşdemir">
  <Navbar />
  
  <main>
    <!-- Hero Section -->
    <section class="hero-section">
      <div class="hero-bg-effects">
        <div class="hero-glow hero-glow-1"></div>
        <div class="hero-glow hero-glow-2"></div>
        <div class="particle particle-1"></div>
        <div class="particle particle-2"></div>
        <div class="particle particle-3"></div>
      </div>
      
      <div class="hero-content animate-fade-in">
        <h1>Hizmetlerim</h1>
        <p>CBS, uzaktan algılama, otomasyon ve web geliştirme alanlarında profesyonel çözümler sunuyorum.</p>
      </div>
    </section>

    <!-- Hizmetler Section -->
    <section class="services-section">
      <div class="section-transition-top"></div>
      <div class="container">
        
        <!-- Hizmet 1: CBS -->
        <div class="service-row scroll-animate">
          <div class="service-content">
            <span class="service-badge">CBS Hizmetleri</span>
            <h2>Coğrafi Bilgi Sistemleri</h2>
            <p>
              ArcGIS Pro, QGIS ve ArcGIS Online kullanarak mekansal analiz, harita üretimi ve CBS projelerinin planlanması ile yürütülmesi hizmetlerini sunuyorum.
            </p>
            <ul class="service-list">
              <li><span class="check-icon">✓</span> Mekansal veri analizi</li>
              <li><span class="check-icon">✓</span> Tematik harita üretimi</li>
              <li><span class="check-icon">✓</span> Uygunluk ve risk analizi</li>
              <li><span class="check-icon">✓</span> Veri dönüştürme ve entegrasyon</li>
            </ul>
          </div>
          <div class="service-image">
            <div class="image-wrapper">
              <img src="/images/hizmetler/cbs.jpg" alt="Coğrafi Bilgi Sistemleri" onerror="this.style.display='none'; this.parentElement.classList.add('fallback')" />
              <div class="image-fallback">
                <span class="fallback-icon">🗺️</span>
                <span class="fallback-text">CBS</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Hizmet 2: Uzaktan Algılama -->
        <div class="service-row reverse scroll-animate">
          <div class="service-content">
            <span class="service-badge">Uzaktan Algılama</span>
            <h2>Uydu Görüntü Analizi</h2>
            <p>
              Landsat ve diğer uydu verileri kullanarak arazi analizi, değişim tespiti ve çevresel izleme çalışmaları gerçekleştiriyorum.
            </p>
            <ul class="service-list">
              <li><span class="check-icon">✓</span> Arazi örtüsü sınıflandırma</li>
              <li><span class="check-icon">✓</span> NDVI ve bitki sağlığı analizi</li>
              <li><span class="check-icon">✓</span> Çok zamanlı değişim tespiti</li>
              <li><span class="check-icon">✓</span> Google Earth Engine projeleri</li>
            </ul>
          </div>
          <div class="service-image">
            <div class="image-wrapper">
              <img src="/images/hizmetler/uzaktan-algilama.jpg" alt="Uzaktan Algılama" onerror="this.style.display='none'; this.parentElement.classList.add('fallback')" />
              <div class="image-fallback">
                <span class="fallback-icon">🛰️</span>
                <span class="fallback-text">Uzaktan Algılama</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Hizmet 3: Otomasyon -->
        <div class="service-row scroll-animate">
          <div class="service-content">
            <span class="service-badge">Otomasyon</span>
            <h2>Workflow & Bot Geliştirme</h2>
            <p>
              n8n ve çeşitli araçlar kullanarak iş süreçlerinizi otomatikleştiren, zaman ve maliyet tasarrufu sağlayan çözümler üretiyorum.
            </p>
            <ul class="service-list">
              <li><span class="check-icon">✓</span> WhatsApp / Telegram bot geliştirme</li>
              <li><span class="check-icon">✓</span> Veri toplama otomasyonu</li>
              <li><span class="check-icon">✓</span> API entegrasyonları (ChatGPT, Google Sheets vb.)</li>
              <li><span class="check-icon">✓</span> İş akışı optimizasyonu</li>
            </ul>
          </div>
          <div class="service-image">
            <div class="image-wrapper">
              <img src="/images/hizmetler/otomasyon.jpg" alt="Otomasyon ve Bot Geliştirme" onerror="this.style.display='none'; this.parentElement.classList.add('fallback')" />
              <div class="image-fallback">
                <span class="fallback-icon">⚡</span>
                <span class="fallback-text">Otomasyon</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Hizmet 4: Web Sitesi -->
        <div class="service-row reverse scroll-animate">
          <div class="service-content">
            <span class="service-badge">Web Geliştirme</span>
            <h2>Kişisel Web Sitesi</h2>
            <p>
              Modern ve hızlı kişisel portfolyo siteleri geliştiriyorum. Profesyonel görünüm, mobil uyumluluk ve kolay içerik yönetimi ile dijital varlığınızı oluşturun.
            </p>
            <ul class="service-list">
              <li><span class="check-icon">✓</span> Modern ve hızlı tasarım</li>
              <li><span class="check-icon">✓</span> Mobil uyumlu (responsive)</li>
              <li><span class="check-icon">✓</span> Admin paneli ile kolay içerik yönetimi</li>
              <li><span class="check-icon">✓</span> İletişim formu ve sosyal medya entegrasyonu</li>
            </ul>
          </div>
          <div class="service-image">
            <div class="image-wrapper">
              <img src="/images/hizmetler/web-sitesi.jpg" alt="Web Sitesi Geliştirme" onerror="this.style.display='none'; this.parentElement.classList.add('fallback')" />
              <div class="image-fallback">
                <span class="fallback-icon">💻</span>
                <span class="fallback-text">Web Geliştirme</span>
              </div>
            </div>
          </div>
        </div>

      </div>
    </section>

    <!-- Süreç Section -->
    <section class="process-section">
      <div class="section-transition-top from-dark"></div>
      <div class="container">
        <h2 class="section-title scroll-animate">Çalışma Sürecim</h2>
        
        <div class="process-grid">
          <!-- Adım 1 -->
          <div class="process-card scroll-animate">
            <div class="process-number">1</div>
            <h3>İhtiyaç Analizi</h3>
            <p>Projenizin gereksinimlerini ve hedeflerini birlikte belirliyoruz.</p>
          </div>

          <!-- Adım 2 -->
          <div class="process-card scroll-animate" style="transition-delay: 100ms;">
            <div class="process-number">2</div>
            <h3>Planlama</h3>
            <p>Zaman çizelgesi ve teknik yaklaşımı planlıyoruz.</p>
          </div>

          <!-- Adım 3 -->
          <div class="process-card scroll-animate" style="transition-delay: 200ms;">
            <div class="process-number">3</div>
            <h3>Uygulama</h3>
            <p>Projeyi geliştiriyor ve düzenli güncellemeler paylaşıyorum.</p>
          </div>

          <!-- Adım 4 -->
          <div class="process-card scroll-animate" style="transition-delay: 300ms;">
            <div class="process-number">4</div>
            <h3>Teslim & Destek</h3>
            <p>Projeyi teslim ediyor ve sonrasında destek sağlıyorum.</p>
          </div>
        </div>

        <!-- Process Timeline (Desktop) -->
        <div class="process-timeline scroll-animate">
          <div class="timeline-line"></div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="section-transition-top"></div>
      <div class="cta-content scroll-animate">
        <h2>Projenizi Konuşalım</h2>
        <p>İhtiyacınıza özel bir çözüm için benimle iletişime geçin. İlk görüşme ücretsizdir.</p>
        <a href="/iletisim" class="cta-button">İletişime Geç</a>
      </div>
    </section>

  </main>
</Layout>

<style>
  /* Hero Section */
  .hero-section {
    padding: 160px 20px 80px;
    background: linear-gradient(135deg, #37353E 0%, #1a1a1a 100%);
    position: relative;
    overflow: hidden;
    text-align: center;
  }

  .hero-bg-effects {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .hero-glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.15;
  }

  .hero-glow-1 {
    width: 400px;
    height: 400px;
    background: #57564F;
    top: -150px;
    left: 50%;
    transform: translateX(-50%);
    animation: pulse-glow 8s ease-in-out infinite;
  }

  .hero-glow-2 {
    width: 300px;
    height: 300px;
    background: #44444E;
    bottom: -100px;
    right: 10%;
    animation: pulse-glow 8s ease-in-out 4s infinite;
  }

  @keyframes pulse-glow {
    0%, 100% { opacity: 0.15; transform: translateX(-50%) scale(1); }
    50% { opacity: 0.25; transform: translateX(-50%) scale(1.1); }
  }

  .particle {
    position: absolute;
    width: 6px;
    height: 6px;
    background: #57564F;
    border-radius: 50%;
    opacity: 0.3;
  }

  .particle-1 {
    top: 30%;
    left: 10%;
    animation: float 15s ease-in-out infinite;
  }

  .particle-2 {
    top: 50%;
    right: 15%;
    animation: float 18s ease-in-out infinite reverse;
  }

  .particle-3 {
    bottom: 20%;
    left: 30%;
    animation: float 12s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0) translateX(0); }
    25% { transform: translateY(-20px) translateX(10px); }
    50% { transform: translateY(-10px) translateX(-10px); }
    75% { transform: translateY(-30px) translateX(5px); }
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 700px;
    margin: 0 auto;
  }

  .hero-content h1 {
    font-size: 48px;
    font-weight: bold;
    color: #D3DAD9;
    margin-bottom: 20px;
  }

  .hero-content p {
    color: #D3DAD9;
    opacity: 0.7;
    font-size: 18px;
    line-height: 1.7;
  }

  /* Section Transitions */
  .section-transition-top {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 100px;
    background: linear-gradient(to bottom, #37353E 0%, transparent 100%);
    pointer-events: none;
  }

  .section-transition-top.from-dark {
    background: linear-gradient(to bottom, #1a1a1a 0%, transparent 100%);
  }

  /* Container */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .section-title {
    font-size: 36px;
    font-weight: bold;
    color: #D3DAD9;
    text-align: center;
    margin-bottom: 60px;
  }

  /* Services Section */
  .services-section {
    padding: 100px 0;
    background-color: #1a1a1a;
    position: relative;
  }

  .service-row {
    display: flex;
    gap: 60px;
    align-items: center;
    margin-bottom: 100px;
    flex-wrap: wrap;
  }

  .service-row:last-child {
    margin-bottom: 0;
  }

  .service-row.reverse {
    flex-direction: row-reverse;
  }

  .service-content {
    flex: 1;
    min-width: 300px;
  }

  .service-badge {
    display: inline-block;
    background-color: #57564F;
    color: #D3DAD9;
    padding: 6px 16px;
    border-radius: 20px;
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 20px;
    transition: all 0.3s ease;
  }

  .service-row:hover .service-badge {
    background-color: #6a695f;
    transform: translateX(5px);
  }

  .service-content h2 {
    font-size: 32px;
    font-weight: bold;
    color: #D3DAD9;
    margin-bottom: 15px;
  }

  .service-content p {
    color: #D3DAD9;
    opacity: 0.7;
    line-height: 1.8;
    margin-bottom: 25px;
  }

  .service-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .service-list li {
    color: #D3DAD9;
    opacity: 0.8;
    padding: 10px 0;
    display: flex;
    align-items: center;
    gap: 12px;
    transition: all 0.3s ease;
  }

  .service-list li:hover {
    opacity: 1;
    transform: translateX(5px);
  }

  .check-icon {
    color: #57564F;
    font-weight: bold;
    transition: transform 0.3s ease;
  }

  .service-list li:hover .check-icon {
    transform: scale(1.2);
  }

  .service-image {
    flex: 1;
    min-width: 300px;
  }

  .image-wrapper {
    border-radius: 16px;
    height: 320px;
    overflow: hidden;
    position: relative;
    background: linear-gradient(135deg, #44444E, #37353E);
    transition: all 0.5s ease;
  }

  .service-row:hover .image-wrapper {
    transform: translateY(-10px);
    box-shadow: 0 25px 50px rgba(0,0,0,0.3);
  }

  .image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .service-row:hover .image-wrapper img {
    transform: scale(1.05);
  }

  .image-fallback {
    position: absolute;
    inset: 0;
    display: none;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 15px;
  }

  .image-wrapper.fallback .image-fallback {
    display: flex;
  }

  .fallback-icon {
    font-size: 64px;
    opacity: 0.5;
  }

  .fallback-text {
    color: #D3DAD9;
    opacity: 0.3;
    font-size: 16px;
  }

  /* Process Section */
  .process-section {
    padding: 100px 0;
    background-color: #37353E;
    position: relative;
  }

  .process-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 30px;
    position: relative;
    z-index: 1;
  }

  .process-card {
    background-color: #44444E;
    border-radius: 16px;
    padding: 35px 30px;
    text-align: center;
    transition: all 0.4s ease;
    position: relative;
  }

  .process-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.2);
  }

  .process-number {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #57564F 0%, #6a695f 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 25px auto;
    color: #D3DAD9;
    font-weight: bold;
    font-size: 24px;
    transition: all 0.3s ease;
  }

  .process-card:hover .process-number {
    transform: scale(1.1) rotate(10deg);
    box-shadow: 0 10px 30px rgba(87, 86, 79, 0.4);
  }

  .process-card h3 {
    color: #D3DAD9;
    font-size: 20px;
    font-weight: 600;
    margin-bottom: 12px;
  }

  .process-card p {
    color: #D3DAD9;
    opacity: 0.6;
    font-size: 15px;
    line-height: 1.7;
  }

  .process-timeline {
    display: none;
    justify-content: center;
    margin-top: 30px;
  }

  .timeline-line {
    height: 4px;
    background: linear-gradient(90deg, transparent 0%, #57564F 20%, #57564F 80%, transparent 100%);
    width: 80%;
    border-radius: 2px;
  }

  @media (min-width: 900px) {
    .process-timeline {
      display: flex;
    }
  }

  /* CTA Section */
  .cta-section {
    padding: 100px 20px;
    background: linear-gradient(135deg, #1a1a1a 0%, #37353E 100%);
    text-align: center;
    position: relative;
  }

  .cta-content {
    position: relative;
    z-index: 1;
    max-width: 600px;
    margin: 0 auto;
  }

  .cta-section h2 {
    font-size: 36px;
    font-weight: bold;
    color: #D3DAD9;
    margin-bottom: 15px;
  }

  .cta-section p {
    color: #D3DAD9;
    opacity: 0.7;
    font-size: 17px;
    margin-bottom: 35px;
    line-height: 1.7;
  }

  .cta-button {
    display: inline-block;
    background-color: #57564F;
    color: #D3DAD9;
    padding: 16px 45px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 500;
    font-size: 16px;
    transition: all 0.3s ease;
  }

  .cta-button:hover {
    background-color: #6a695f;
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 15px 40px rgba(87, 86, 79, 0.3);
  }

  /* Animations */
  .animate-fade-in {
    animation: fadeIn 1s ease-out forwards;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* Scroll Animations */
  .scroll-animate {
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 0.7s ease-out, transform 0.7s ease-out;
  }

  .scroll-animate.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-content h1 {
      font-size: 36px;
    }

    .service-row,
    .service-row.reverse {
      flex-direction: column;
    }

    .service-content h2 {
      font-size: 26px;
    }

    .section-title {
      font-size: 28px;
    }

    .cta-section h2 {
      font-size: 28px;
    }

    .service-row:hover .image-wrapper {
      transform: translateY(-5px);
    }
  }
</style>

<script>
  // Scroll Animation with Intersection Observer
  const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.1
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, observerOptions);

  document.addEventListener('DOMContentLoaded', () => {
    const animatedElements = document.querySelectorAll('.scroll-animate');
    animatedElements.forEach(el => observer.observe(el));
  });

  // Smooth scroll for anchor links
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
</script>